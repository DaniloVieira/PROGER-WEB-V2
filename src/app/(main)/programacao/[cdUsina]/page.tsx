"use client";

import { ProgramacaoContent } from "@/components/programacao/programacao-content";

// ─── Programação Page (with TabShell) ────────────────────────────────────────
// Rendered inside the (main) group layout which provides TabShell.

export default function ProgramacaoPage() {
	return <ProgramacaoContent />;
}
